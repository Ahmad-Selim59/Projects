This is a retrieveal engine which allows the user to choose a txt file on their computer with different articles it would then allow the user to search for an article
containing a specific work and it will allow them to see every article containing that word
